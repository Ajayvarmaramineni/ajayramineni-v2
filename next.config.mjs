/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add specific hostnames here if you load remote images (e.g. "images.unsplash.com")
    remotePatterns: [],
  },
};

export default nextConfig;
